version: '3'

services: 
    reminders-database:
        environment:
            - SA_PASSWORD=123Aa321
            - ACCEPT_EULA=Y
        ports:
            - "7000:1433"
    reminders-build:
        env_file: 
            - .env
    reminders-mvc:
        env_file: 
            - .env
        ports:
            - "5000:80"
    reminders-api:
        env_file: 
            - .env
        ports:
            - "6000:80"